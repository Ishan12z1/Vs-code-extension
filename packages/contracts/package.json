{
  "name": "@control-agent/contracts",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc -p .",
    "watch": "tsc -w -p .",
    "test": "node -e \"console.log('No contracts tests wired yet. This will be replaced later.')\"",
    "lint": "eslint \"src/**/*.ts\"",
    "package": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^5.0.0"
  }
}
