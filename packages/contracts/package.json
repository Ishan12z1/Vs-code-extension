{
  "name": "@control-agent/contracts",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rimraf dist dist-tests",
    "build": "tsc -p .",
    "build:test": "tsc -p tsconfig.test.json",
    "watch": "tsc -w -p .",
    "test": "npm run build:test && node --test \"dist-tests/tests/**/*.js\"",
    "lint": "eslint \"src/**/*.ts\" \"tests/**/*.ts\"",
    "package": "npm run build"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "rimraf": "^5.0.0",
    "@types/node": "^20.17.0"
  }
}
