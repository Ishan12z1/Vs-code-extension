# Local setup

This project uses:

- npm workspaces for Typescript packages
- FastAPI for the backend API
- Docker Compose for local Postgres

## Prerequisistes:

Install these first :

- Node.js
- Python 3.12+
- Docker Desktop or Docker Enginer
- Git

## Initial install

From the repo root:

```bash
npm install
```
