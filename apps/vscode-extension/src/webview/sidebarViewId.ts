/**
 * Stable IDs for the extension sidebar shell.
 *
 * Keep these centralized so command wiring, provider registration,
 * and package.json stay aligned.
 */
export const CONTROL_AGENT_VIEW_CONTAINER_ID = "controlAgent";
export const CONTROL_AGENT_SIDEBAR_VIEW_ID = "controlAgent.sidebarView";
