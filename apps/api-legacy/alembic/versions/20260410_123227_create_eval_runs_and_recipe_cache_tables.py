"""create eval_runs and recipe_cache tables

Revision ID: 3e5551208895
Revises: fe717de5f9a0
Create Date: 2026-04-10 12:32:27.668584
"""

from __future__ import annotations

import sqlalchemy as sa

from alembic import op

# Revision identifiers, used by Alembic.
revision = "3e5551208895"
down_revision = "fe717de5f9a0"
branch_labels = None
depends_on = None


def upgrade() -> None:
    """
    Apply this migration.

    C4 uses this template so later revisions in C5+ have a clean, typed starting point.
    """
    # ### commands auto generated by Alembic - please adjust! ###
    op.create_table(
        "eval_runs",
        sa.Column(
            "id", sa.Uuid(), nullable=False, comment="Primary identifier for the evaluation run."
        ),
        sa.Column(
            "suite_name",
            sa.String(length=100),
            nullable=False,
            comment="Name of the evaluation suite that was executed.",
        ),
        sa.Column(
            "planner_version",
            sa.String(length=100),
            nullable=True,
            comment="Planner/provider version evaluated during this run.",
        ),
        sa.Column(
            "results_json",
            sa.JSON(),
            nullable=False,
            comment="Structured evaluation results payload.",
        ),
        sa.Column(
            "score",
            sa.Float(),
            nullable=True,
            comment="Optional aggregate score for the evaluation run.",
        ),
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            server_default=sa.text("now()"),
            nullable=False,
            comment="UTC time when this row was created.",
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            server_default=sa.text("now()"),
            nullable=False,
            comment="UTC time when this row was last updated.",
        ),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_table(
        "recipe_cache",
        sa.Column(
            "id", sa.Uuid(), nullable=False, comment="Primary identifier for the cached recipe row."
        ),
        sa.Column(
            "recipe_key",
            sa.String(length=255),
            nullable=False,
            comment="Stable cache key for the recipe/template payload.",
        ),
        sa.Column(
            "source",
            sa.String(length=100),
            nullable=False,
            comment="Source of the cached recipe payload, such as mcp or local_template.",
        ),
        sa.Column(
            "payload_json",
            sa.JSON(),
            nullable=False,
            comment="Structured cached recipe/template payload.",
        ),
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            server_default=sa.text("now()"),
            nullable=False,
            comment="UTC time when this row was created.",
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            server_default=sa.text("now()"),
            nullable=False,
            comment="UTC time when this row was last updated.",
        ),
        sa.PrimaryKeyConstraint("id"),
    )
    op.create_index(
        op.f("ix_recipe_cache_recipe_key"), "recipe_cache", ["recipe_key"], unique=False
    )
    # ### end Alembic commands ###


def downgrade() -> None:
    """
    Revert this migration.

    Every migration should keep downgrade logic explicit, even early in the project.
    """
    # ### commands auto generated by Alembic - please adjust! ###
    op.drop_index(op.f("ix_recipe_cache_recipe_key"), table_name="recipe_cache")
    op.drop_table("recipe_cache")
    op.drop_table("eval_runs")
    # ### end Alembic commands ###
